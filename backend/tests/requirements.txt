# Barcode / OCR benchmark suite
zxing-cpp==3.1.1           # barcode decoder (the recommended one)
opencv-python-headless     # only for cv2.barcode, the comparison baseline
rapidocr-onnxruntime==1.2.3  # OCR: PP-OCR models on onnxruntime, CPU-only
pillow
numpy
